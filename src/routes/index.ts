import {LoginPage, RegisterPage} from "../pages";

const routes = [
    {
        path: '/login',
        component: LoginPage
    },
    {
        path: '/register',
        component: RegisterPage
    }
]

export {routes};